spring:
  application:
    name: delivery-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
      
  jpa:
    open-in-view: false
    properties:
      hibernate:
        default_schema: delivery_schema

