spring:
  application:
    name: user-service

  # local 환경 실행 기분
  profiles:
    default: local

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        default_schema: user_schema
        format_sql: true
        highlight_sql: true

    open-in-view: false
